{
  "name": "biteback-designs",
  "version": "1.0.0",
  "description": "BiteBack design mockups — built with Eleventy",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --port 8080",
    "build": "npx @11ty/eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
