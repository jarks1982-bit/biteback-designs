---
layout: onboarding.njk
title: Pick Your Name
screenTitle: "Step 3 — Pick Your Name"
step: 3
ctaText: ""
---

<style>
  .preview-card {
    background: var(--card-bg); border: 1px solid var(--card-border); border-radius: 20px;
    padding: 24px; text-align: center; margin-bottom: 24px;
  }
  .avatar-placeholder {
    width: 64px; height: 64px; border-radius: 50%;
    background: var(--card-border); border: 2px dashed #444;
    display: flex; align-items: center; justify-content: center;
    font-size: 24px; margin: 0 auto 12px;
  }
  .avatar-next-hint {
    font-family: var(--font-mono); font-size: 8px; letter-spacing: 1px;
    color: var(--clutch); text-align: center; margin-top: 6px;
    animation: hint-pulse 2s ease-in-out infinite;
  }
  @keyframes hint-pulse {
    0%, 100% { opacity: 0.6; }
    50% { opacity: 1; }
  }
  .preview-name {
    font-family: var(--font-display); font-weight: 700; font-size: 28px;
    letter-spacing: 2px; color: var(--text-primary);
    min-height: 36px; margin-top: 4px;
  }
  .preview-tier {
    font-family: var(--font-mono); font-size: 10px; letter-spacing: 1px;
    color: var(--text-tertiary); margin-top: 2px;
  }
  .preview-rank {
    display: inline-block; margin-top: 8px; padding: 4px 12px;
    background: rgba(255,215,0,0.08); border: 1px solid rgba(255,215,0,0.12);
    border-radius: 20px;
    font-family: var(--font-mono); font-size: 9px; letter-spacing: 1px; color: #FFD700;
  }
  .input-box {
    width: 100%; padding: 16px 18px; border-radius: 14px;
    background: var(--card-bg); border: 1.5px solid var(--clutch);
    font-family: var(--font-body); font-size: 18px; font-weight: 600;
    color: var(--text-primary); letter-spacing: 1px;
    text-align: center; margin-bottom: 8px;
  }
  .name-hint {
    font-family: var(--font-mono); font-size: 10px; color: var(--text-tertiary);
    text-align: center; letter-spacing: 1px; margin-bottom: 20px;
  }
  .name-continue-btn {
    width: 100%; padding: 16px; border-radius: 14px;
    background: var(--clutch); text-align: center;
    font-family: var(--font-display); font-weight: 700; font-size: 16px;
    letter-spacing: 2px; color: #0A0A0A;
    margin-top: auto; margin-bottom: 20px; cursor: pointer;
  }
</style>

<div style="flex: 1; display: flex; flex-direction: column; padding-top: 20px;">
  <div class="heading" style="text-align: center; margin-bottom: 6px;">WHAT SHOULD WE CALL YOU?</div>
  <div class="body-text" style="text-align: center; margin-bottom: 32px;">This is how you'll show up<br/>on the leaderboard</div>

  <div class="preview-card">
    <div class="avatar-placeholder">👤</div>
    <div class="preview-name">BraceGod</div>
    <div class="preview-tier">SNAGGLEFANG</div>
    <div class="preview-rank">🏆 RANK #—</div>
  </div>

  <div style="font-family: var(--font-mono); font-size: 10px; color: var(--text-tertiary); text-align: center; letter-spacing: 1px; margin-top: -16px; margin-bottom: 24px;">You'll choose an avatar next</div>

  <div class="input-box">BraceGod</div>
  <div class="name-hint">Max 16 characters · No real names</div>
  <div class="name-continue-btn">NEXT</div>
</div>
