---
layout: onboarding.njk
title: Your Challenges
screenTitle: "Step 7 — Your Challenges (QR Scanned)"
step: 7
ctaText: "NEXT"
---

<style>
  .practice-badge {
    display: flex; align-items: center; justify-content: center; gap: 8px;
    background: rgba(191,255,0,0.04); border: 1px solid rgba(191,255,0,0.12);
    border-radius: 12px; padding: 10px 16px; margin-bottom: 16px;
  }
  .practice-badge-text {
    font-family: var(--font-mono); font-size: 10px; letter-spacing: 1px; color: var(--clutch);
  }
  .section-row {
    display: flex; align-items: center; justify-content: space-between;
    margin-bottom: 10px;
  }
  .section-label {
    font-family: var(--font-mono); font-size: 10px; letter-spacing: 2px; color: var(--text-tertiary);
  }
  .active-count {
    font-family: var(--font-mono); font-size: 10px; letter-spacing: 1px; color: var(--clutch);
  }
  .challenge-card {
    display: flex; align-items: center; gap: 14px;
    background: var(--card-bg); border: 1px solid var(--card-border); border-radius: 16px;
    padding: 14px 16px; margin-bottom: 8px;
  }
  .challenge-card.on {
    border-color: rgba(191,255,0,0.3);
    background: rgba(191,255,0,0.03);
  }
  .ch-emoji { font-size: 26px; flex-shrink: 0; }
  .ch-info { flex: 1; }
  .ch-name {
    font-family: var(--font-body); font-size: 15px; font-weight: 600; color: var(--text-primary);
  }
  .ch-desc {
    font-family: var(--font-mono); font-size: 9px; color: var(--text-tertiary);
    letter-spacing: 1px; margin-top: 2px;
  }
  .toggle {
    width: 44px; height: 26px; border-radius: 13px;
    background: #2A2A2A; position: relative; flex-shrink: 0;
  }
  .toggle.on { background: var(--clutch); }
  .toggle-knob {
    width: 20px; height: 20px; border-radius: 50%;
    background: #fff; position: absolute; top: 3px;
  }
  .toggle:not(.on) .toggle-knob { left: 3px; }
  .toggle.on .toggle-knob { left: 21px; }
  .bottom-note {
    font-family: var(--font-mono); font-size: 10px; color: var(--text-tertiary);
    text-align: center; letter-spacing: 1px;
    margin-top: 4px; margin-bottom: 8px;
  }
</style>

<div style="flex: 1; display: flex; flex-direction: column; padding-top: 10px;">
  <div class="heading" style="text-align: center; margin-bottom: 4px;">YOUR CHALLENGES</div>
  <div class="body-text" style="text-align: center; margin-bottom: 20px;">Your ortho set these up for you</div>

  <div class="practice-badge">
    <div class="practice-badge-text">SMILE STUDIO ORTHO</div>
  </div>

  <div class="section-row">
    <div class="section-label">FROM YOUR PRACTICE</div>
    <div class="active-count">2 / 3 ACTIVE</div>
  </div>

  <div class="challenge-card on">
    <div class="ch-emoji">😁</div>
    <div class="ch-info">
      <div class="ch-name">Wear Aligners</div>
      <div class="ch-desc">20+ HOURS DAILY</div>
    </div>
    <div class="toggle on"><div class="toggle-knob"></div></div>
  </div>

  <div class="challenge-card on">
    <div class="ch-emoji">🦷</div>
    <div class="ch-info">
      <div class="ch-name">Brush &amp; Floss</div>
      <div class="ch-desc">TWICE DAILY</div>
    </div>
    <div class="toggle on"><div class="toggle-knob"></div></div>
  </div>

  <div class="challenge-card">
    <div class="ch-emoji">🔗</div>
    <div class="ch-info">
      <div class="ch-name">Rubber Bands</div>
      <div class="ch-desc">AS PRESCRIBED</div>
    </div>
    <div class="toggle"><div class="toggle-knob"></div></div>
  </div>

  <div class="bottom-note">Max 3 challenges · Change anytime in settings</div>
</div>
